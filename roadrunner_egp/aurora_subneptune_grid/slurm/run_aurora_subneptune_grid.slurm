#!/bin/bash
# DEPRECATED — use scripts/submit_two_stage_grid.sh <model_name> instead.
echo "ERROR: Single-stage grid runs re-do climate for every phase." >&2
echo "Use: bash roadrunner_egp/aurora_subneptune_grid/scripts/submit_two_stage_grid.sh \$(pwd) aurora_subneptune_v0" >&2
exit 1
